---
import { Clock } from 'lucide-astro';

interface Props {
	minutes: number;
}

const { minutes } = Astro.props;
---

<span class="inline-flex items-center gap-1 text-sm text-base-content/80">
	<Clock class="w-4 h-4" />
	<span>{minutes} min read</span>
</span>
